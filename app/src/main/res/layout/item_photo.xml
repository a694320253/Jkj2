<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/w_120_dp"
    android:layout_gravity="center"
    android:background="@color/black0"
    android:gravity="center"
    android:padding="@dimen/p_1_dp"
    android:tag="click_item">

    <ImageView
        android:id="@+id/iv_pic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_photo_loading" />

    <TextView
        android:id="@+id/tv_selector"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_margin="@dimen/p_10_dp"
        android:background="@drawable/photo_cb_selector"
        android:gravity="center"
        android:textColor="@color/white"
        android:textSize="@dimen/text_12_sp" />

    <TextView
        android:id="@+id/tv_camera"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:drawablePadding="@dimen/p_5_dp"
        android:drawableTop="@drawable/pic_camera"
        android:gravity="center"
        android:text="@string/take_photo_text"
        android:textColor="@color/white"
        android:textSize="@dimen/text_12_sp" />
</RelativeLayout>
