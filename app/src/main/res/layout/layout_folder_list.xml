<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/dim_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000" />

    <ListView
        android:id="@+id/lv_folder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?android:attr/actionBarSize"
        android:background="#ffffff"
        android:divider="#e0e0e0"
        android:dividerHeight="1dp"
        android:listSelector="@drawable/layout_selector"
        android:overScrollMode="never"
        android:paddingBottom="@dimen/p_2_dp"
        android:paddingStart="@dimen/p_10_dp"
        android:paddingEnd="@dimen/p_10_dp"
        android:paddingTop="@dimen/p_2_dp"
        android:scrollbarSize="@dimen/p_3_dp"
        android:scrollbarThumbVertical="@drawable/scrollbar_vertical_thumb"
        android:scrollbars="none" />
</FrameLayout>

